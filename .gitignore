# Local-only design/planning material (not committed)
design/

# Secrets
.env
.env.*
!.env.example

# Local keys (Google service account JSON, etc.) — never commit the actual keys
keys/*
!keys/.gitkeep
!keys/README.md

# Dependencies / build
node_modules/
dist/

# Collector output (local JSON)
output/
